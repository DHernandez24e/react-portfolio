import React from 'react';
import Nav from '../Navigation';

function Header() {

    return(
        <header>
            This is a header
            <Nav></Nav>
        </header>
    );
}

export default Header;